# CLAUDE.md — โปรเจกต์ PK Lab (เว็บสอนเภสัชจลนศาสตร์ ภาษาไทย)

ไฟล์นี้ให้ Claude Code อ่านอัตโนมัติทุกเซสชัน เพื่อเข้าใจบริบทและกติกาของโปรเจกต์
รายละเอียดเต็ม (ประวัติการพัฒนา + เอกสารอ้างอิงครบ + ชุด quiz) อยู่ใน `HANDOFF_PK_Lab.md`

---

## ภาพรวม
- เว็บปฏิบัติการเภสัชจลนศาสตร์ (PK) ภาษาไทย สำหรับนักศึกษาเภสัชศาสตร์
- เป้าหมายคู่ขนาน: (1) สื่อการสอนแบบ interactive (2) งานวิจัยในชั้นเรียนเพื่อตีพิมพ์ (within-subject pre–post, แบบสอบถาม TAM, ยื่น EC, วารสารเป้าหมาย AJPE / Pharmacy Education)
- เทคโนโลยี: HTML + vanilla JS + custom SVG ล้วน **ไม่มีไลบรารีภายนอก** (โหลดเฉพาะฟอนต์ Google: Mitr + Sarabun)

## สถาปัตยกรรมไฟล์ (สำคัญที่สุด)
โครงสร้างปัจจุบันเป็น **หลายไฟล์ในโฟลเดอร์เดียว** (เพิ่งย้ายมาจากไฟล์เดียวในเซสชันก่อน):
- `index.html` = "เปลือก" แถบแท็บ — ลำดับ/ชื่อแท็บกำหนดที่ลิสต์ `TABS`; โหลดแต่ละหัวข้อผ่าน `<iframe>` (lazy, สร้างเมื่อกดแท็บ)
- ไฟล์หัวข้อ = ไฟล์ละ 1 แท็บ ทำงานได้ด้วยตัวเอง (standalone) เช่น `order-nl5.html`, `vd.html`, `absorption.html`, `firstpass.html`, `clearance.html`, `halflife.html`, `dosing.html`, `template.html`
- `pk-lab.html` = ไฟล์เดิมที่ยังถือหัวข้อ PK ที่ยังไม่ได้แยก กำลังทยอยแยกออกทีละหัวข้อ → สุดท้ายจะ retire หรือทำเป็นหน้าแนะนำ/สารบัญ
- **กฎทอง: เนื้อหาหัวข้อหนึ่ง ๆ อยู่ใน "ไฟล์ของหัวข้อนั้น" ไม่ใช่ใน index.html** → แก้เนื้อหา = แก้ไฟล์หัวข้อ; แตะ index.html เฉพาะตอนเปลี่ยนชื่อ/ลำดับ/เพิ่ม-ลบแท็บ

## วิธีทำงานกับแท็บ
- **เพิ่มแท็บ:** วางไฟล์ `.html` ในโฟลเดอร์ + เพิ่ม 1 บรรทัดในลิสต์ `TABS` ของ index.html
- **จัดลำดับ/เปลี่ยนชื่อแท็บ:** แก้ลำดับ/label ในลิสต์ `TABS` เท่านั้น (ไม่แตะไฟล์เนื้อหา)
- **แยกหัวข้อจาก pk-lab.html ออกเป็นแท็บ (สูตรปลอดภัย 4 ขั้น):**
  1. คัดลอกหัวข้อออกเป็นไฟล์ standalone — ยกมาครบ: HTML ของหัวข้อ + สคริปต์ widget/quiz + CSS ที่ใช้ — แล้วเพิ่มเป็นแท็บ โดย**ยังไม่แตะ pk-lab.html**
  2. ทดสอบ (เปิดไฟล์ standalone ตรง ๆ + เปิดผ่านแท็บ)
  3. เมื่อใช้งานได้ดี ค่อยลบหัวข้อออกจาก pk-lab.html
  4. ตรวจว่าหัวข้อที่เหลือใน pk-lab.html ยังทำงานครบ
- **กัน id ชนกัน:** id ในแต่ละไฟล์ควร prefix เฉพาะ เช่น `nl-`, `vd-`, `quizvd-`

## การทดสอบ
- เปิดผ่าน **local web server เท่านั้น** — เปิดแบบ `file://` (ดับเบิลคลิก index.html) เบราว์เซอร์จะบล็อก iframe ทำให้แท็บว่าง
  - รัน: `python3 -m http.server 8000` แล้วเปิด `http://localhost:8000`
- ไฟล์ standalone (เช่น `vd.html`) เปิดเดี่ยว ๆ ได้เลย (ไม่มี iframe)
- หลังแก้ทุกครั้ง: refresh localhost แล้วตรวจ widget/quiz/กราฟจริง

## การ Deploy
- ปลายทาง: **GitHub Pages** ผ่าน repo `Spakawadee/pk-lab` (อัปโหลดทั้งโฟลเดอร์ → เปิด Pages ใน Settings) เมื่อ deploy แล้วปัญหา file:// หายถาวร

## กติกาประจำ (ห้ามละเมิด)
- **ความถูกต้องเป็นเรื่องห้ามต่อรอง:** ค่ายา/พารามิเตอร์ทุกตัวต้องมาจากแหล่งวิชาการ พร้อมอ้างอิง **Vancouver + ลิงก์ (PMID/DOI)** — ห้ามแต่งตัวเลขให้ดูแม่น ถ้าไม่แน่ใจให้ทำเครื่องหมายและแจ้ง
- **ไม่ regenerate เนื้อหาที่ verify แล้ว** — คัดลอก/ย้ายของเดิม ไม่เขียนใหม่จากศูนย์
- ภาษาไทยเชิงวิชาการ
- อธิบายสิ่งที่แก้ทีละขั้นแบบเข้าใจง่าย (ผู้สอนไม่มีพื้นฐานโค้ด ต้องดูแลไฟล์เองได้)
- ทำทีละเครื่องมือ/ทีละหัวข้อ — เสนอแนวทาง → รอผู้สอนอนุมัติ → ค่อยลงมือ
- เกรด/การประเมินอยู่ใน Moodle ไม่รวมในเว็บนี้
- ทุกครั้งที่จะแก้ไฟล์ ให้แสดง diff แล้วรออนุมัติ

## Convention สัญลักษณ์
- ชีวประสิทธิผลแบบ first-pass: **F = Fa × Fg × Fh** (ตัว F ใหญ่ทั้งสามตัว ตัวห้อยเล็ก a/g/h)
  - Fa = สัดส่วนที่ถูกดูดซึมจาก lumen เข้าสู่ enterocyte
  - Fg = สัดส่วนที่รอดจาก first-pass ที่ผนังลำไส้
  - Fh = สัดส่วนที่รอดจาก first-pass ที่ตับ
  - (เป็นรูปแบบที่พบมากสุดในวรรณกรรม PK; เลี่ยง "fa" ตัวเล็ก และเลี่ยง "FA FG FH" ตัวใหญ่หมด)

## ระดับการสอน / ข้อจำกัดเนื้อหา
- lab พื้นฐานมีเวลา **~2 ชั่วโมง** — เนื้อหาต้องไม่ล้น
- แท็บ **zero/first-order · linear/nonlinear:** concept ล้วน — **ไม่แสดงสมการ Michaelis–Menten, ไม่โชว์ Vmax/Km บนหน้าจอ** (โมเดลใช้ค่าเหล่านี้ภายในโค้ดได้ แต่ไม่แสดง)
- แท็บ **First-pass:** ทำให้ง่ายที่สุด เน้นแค่ "first-pass เกิดที่ใด + จากปัจจัย/เอนไซม์ใด" (เพราะมีวิชา Basic PK เทอม 2 อยู่แล้ว); คง widget แบบง่าย (toggle เปิด-ปิดแต่ละด่าน + ผลเชิงคุณภาพ มาก/ปานกลาง/น้อย) — ไม่ลงตัวเลข F% ละเอียด และไม่แสดงสมการ

## ค่ายาที่ตรวจสอบแล้ว (ใช้ค่าเหล่านี้ ห้ามแก้โดยไม่มีแหล่งอ้างอิง)
ใช้ในแท็บ zero/first-order (`order-nl5.html`) — คำนวณภายใน ไม่แสดงสมการบนจอ:
- **phenytoin (nonlinear):** Css = R·Km / (Vmax − R); default Vmax ≈ 490 mg/วัน (≈7 mg/kg/วัน), Km ≈ 6 mg/L (อยู่ในช่วงรายงาน 6.4–6.7); ช่วงอ้างอิง 10–20 mg/L; สไลเดอร์ขนาดยา 50–470 mg/วัน
- **levetiracetam (linear):** Css = R / CL; CL ≈ 91 L/วัน (≈ 3.8 L/ชม. ≈ 0.9 mL/min/kg) → เส้นตรง; ช่วงอ้างอิง 12–46 mg/L; สไลเดอร์ขนาดยา 250–3500 mg/วัน
- พารามิเตอร์กราฟการลดลงหลัง IV: C0 = 20 mg/L, first-order k = 0.35/ชม., zero-order K0 = 2.2 mg/L/ชม., ช่วง 12 ชม.
- ⚠️ **การแก้สำคัญ:** PMID 2714918 ผู้แต่งคือ **el-Sayed YM, Islam SI** (ไม่ใช่ "Bachmann KA" ที่เคยพิมพ์พลาด)
- (รายการอ้างอิง Vancouver ฉบับเต็มดูใน HANDOFF_PK_Lab.md)

## บั๊กที่แก้แล้ว — อย่าให้กลับมา
- กราฟ phenytoin (nonlinear) เคยมี **"หักมุม" ที่ขอบบน** เพราะโค้ดลากเส้นเลียบขอบบนก่อนหยุด → แก้โดย interpolate จุดที่ Css แตะขอบบน (ymax) แล้วจบเส้นที่ขอบบนพอดี (ออกขอบบนเรียบ); จุด operating point แสดงเฉพาะเมื่อค่าอยู่ในกราฟ
- ตรวจไวยากรณ์ทุก `<script>` ก่อนเสมอ และ **ตัดคอมเมนต์ HTML ออกก่อนตรวจ** (มีคำว่า `<script>` อยู่ในคอมเมนต์ใกล้ต้นไฟล์ ถ้าไม่ตัดจะ false positive)
